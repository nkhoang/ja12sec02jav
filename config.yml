# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: Meaning
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: content
      external_name: content
      # Type: String Stats: 3495 properties of this type in this kind.

    - property: examples
      external_name: examples
      # Type: String Stats: 3491 properties of this type in this kind.

    - property: kindId
      external_name: kindId
      # Type: Integer Stats: 3495 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: type
      external_name: type
      # Type: String Stats: 354 properties of this type in this kind.


- kind: User
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: accountExpired
      external_name: accountExpired
      # Type: Boolean Stats: 1 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: accountLocked
      external_name: accountLocked
      # Type: Boolean Stats: 1 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: credentialExpired
      external_name: credentialExpired
      # Type: Boolean Stats: 1 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: enabled
      external_name: enabled
      # Type: Boolean Stats: 1 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: firstName
      external_name: firstName
      # Type: String Stats: 1 properties of this type in this kind.

    - property: lastName
      external_name: lastName
      # Type: String Stats: 1 properties of this type in this kind.

    - property: password
      external_name: password
      # Type: String Stats: 1 properties of this type in this kind.

    - property: roleNames
      external_name: roleNames
      # Type: String Stats: 2 properties of this type in this kind.

    - property: username
      external_name: username
      # Type: String Stats: 1 properties of this type in this kind.


- kind: Word
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

	  
    - property: description
      external_name: description
      # Type: String Stats: 309 properties of this type in this kind.

    - property: meaningIds
      external_name: meaningIds
      # Type: Integer Stats: 3495 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: pron
      external_name: pron
      # Type: String Stats: 284 properties of this type in this kind.

    - property: soundSource
      external_name: soundSource
      # Type: String Stats: 285 properties of this type in this kind.

    - property: timeStamp
      external_name: timeStamp
      # Type: Integer Stats: 309 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


